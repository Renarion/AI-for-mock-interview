# Copy this file to .env and adjust values for your environment
# cp .env.example .env

# Public API URL for the frontend build
NEXT_PUBLIC_API_URL=https://analyticsinterview.live/api

# Where the frontend is hosted (used by backend for CORS/Cookies)
FRONTEND_URL=https://analyticsinterview.live

# Secret key for signing JWT tokens (override in backend/.env if needed)
SECRET_KEY=change-me-in-production
